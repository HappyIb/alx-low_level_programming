#include "main.h"

/**
 * _isdigit - checks whether a character is a digit/number(0-9) or not.
 * @i: int to be checked
 * Return: 1 if i is a digit, 0 otherwise
 */

int _isdigit(int i)
{
	return (i >= '0' && i <= '9');
}
